# web

Vite + React 19 + TypeScript. Part of the HackSpain 2026 template — see the
root `../README.md` and `../CHEATSHEET.md`.

```
pnpm --filter web run dev      # or: make web, from the repo root
```
