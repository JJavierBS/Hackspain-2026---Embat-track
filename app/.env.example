# Copy to .env and adjust if your local Postgres differs from the defaults.
# The backend reads these directly (see backend/src/main/resources/application.yml).

DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=app
DB_USER=app
DB_PASSWORD=hackathon
SERVER_PORT=8080
