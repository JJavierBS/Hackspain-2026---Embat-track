notas.txt

# macOS artifacts
.DS_Store
.AppleDouble
.LSOverride
__MACOSX/
._*

# Archives
*.zip
*.tar.gz
*.tgz

# Node
node_modules/
.pnpm-store/

# Backend (Java / Maven)
backend/target/
**/target/

# Mobile / Expo
mobile/.expo/
**/.expo/

# Logs
*.log

# Data (CSVs and DuckDB file) never go to git. The slice the image ships is committed
# under backend/demo/ instead, inside the Docker build context (docs/DEPLOY.md).
data/

# Frontend build
frontend/dist/

# Local env
.env

# Python bytecode
__pycache__/
*.pyc

# npm is the package manager here: a second lockfile changes how Vercel builds.
frontend/pnpm-lock.yaml

# Coding-agent and design-tool working files (local only, never part of the deliverable)
CLAUDE.md
.claude/
.cursor/
.impeccable/
docs/superpowers/
