notas.txt

# macOS artifacts
.DS_Store
.AppleDouble
.LSOverride
__MACOSX/
._*

# Archives
*.zip
*.tar.gz
*.tgz

# Node
node_modules/
.pnpm-store/

# Backend (Java / Maven)
backend/target/
**/target/

# Mobile / Expo
mobile/.expo/
**/.expo/

# Logs
*.log

# Data (CSVs and the full DuckDB file) never go to git. The demo slice does: the deployed
# image builds from the repo and has no other way to get it (docs/DEPLOY.md).
data/*
!data/xray-demo.duckdb.gz

# Frontend build
frontend/dist/

# Local env
.env

# Impeccable review captures (local only)
.impeccable/review/
